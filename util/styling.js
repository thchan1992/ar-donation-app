export const headerColor = '#FF304F';

export const bgColor = '#002651';

export const buttonColor = '#28C7FA';

export const borderColor = '#FF304F';

export const instrucText = {
  instrucText: {
    alignSelf: 'center',
    // padding: 5,
    fontWeight: 'bold',
    fontSize: 25,
    color: 'white',
  },
};

export const userSpeech = {
  userSpeech: {
    alignSelf: 'center',
    padding: 5,
    fontSize: 17,
    color: '#775ADA',
  },
};

export const image = {
  image: {
    paddingTop: 40,
    width: 300,
    height: 300,
    borderWidth: 3,
    borderColor: borderColor,
    borderRadius: 8,
    padding: 30,
  },
};

export const infoText = {
  infoText: {
    padding: 10,
    fontSize: 30,
    color: 'white',
  },
};

import {StyleSheet} from 'react-native';
export const horizontalLine = {
  horizontalLine: {
    borderBottomColor: headerColor,
    borderBottomWidth: StyleSheet.hairlineWidth,
  },
};
